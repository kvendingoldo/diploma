\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{doclad}

\ProcessOptions

\RequirePackage[T2A]{fontenc}
\RequirePackage[utf8]{inputenc}
\RequirePackage[russian]{babel}

%\def\refer{\color{-blue!50}}
%\def\remark{\color{-green!10}}
%\def\defin{\color{magenta}}
%\def\comment{\color{violet}}
\definecolor{UniBlue}{RGB}{83,121,170}
  
\mode<presentation> {
  \setbeamertemplate{navigation symbols}{}
  \usetheme{CambridgeUS}
  %\usecolortheme{dolphin}
  \setbeamercovered{transparent}
  %\setbeamercolor{title}{fg=blue!90!black,bg=black!10!white}
  \setbeamercolor{title}{fg=UniBlue}
  \setbeamercolor{frametitle}{fg=UniBlue}
  \setbeamercolor{structure}{fg=UniBlue}
}

\RequirePackage{animate}

\hypersetup{colorlinks=true}

\RequirePackage{indentfirst} % Делать отступ в начале параграфа
\sloppy % Избавляемся от переполнений
\clubpenalty=10000 % Запрещаем разрыв страницы после первой строки абзаца
\widowpenalty=10000 % Запрещаем разрыв страницы после последней строки абзаца
\righthyphenmin=2 % Минимальное число символов при переносе - 2.
\RequirePackage{cmap} % Улучшенный поиск русских слов в полученном pdf-файле

\RequirePackage{fancyvrb}

\RequirePackage{amsmath,amssymb,amsfonts,latexsym,mathtext,mathtools}

\RequirePackage{graphicx}\graphicspath{{fig/}}

\definecolor{labelcolor}{RGB}{100,0,0}

\RequirePackage{xtab}
